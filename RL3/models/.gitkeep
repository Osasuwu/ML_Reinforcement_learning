# Директория для сохранения обученных моделей

После обучения здесь будут сохранены:
- `best_model.zip` - лучшая модель по eval метрике
- `ppo_robot_visual_final.zip` - финальная модель после завершения обучения
- `ppo_robot_visual_*_steps.zip` - чекпоинты каждые 10,000 шагов

Модели исключены из Git (.gitignore) из-за большого размера.

